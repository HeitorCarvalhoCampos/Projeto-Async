<div class="container mt-5 d-flex justify-content-center">
  <div class="card shadow-lg border-0 bg-dark text-light" 
       style="max-width: 600px; width: 100%; border-radius: 15px;">
    <div class="card-body p-4">
      <h2 class="text-center text-white mb-4">
        💡 Criar Nova Ideia
      </h2>

      <form action="/ideas/create" method="POST">
        <!-- Título -->
        <div class="mb-3">
          <label for="title" class="form-label fw-semibold text-white">
            <i class="bi bi-lightbulb-fill me-1 text-warning"></i>
            Título
          </label>
          <input 
            type="text" 
            name="title" 
            id="title" 
            class="form-control form-control-lg border-0 bg-secondary text-light shadow-sm" 
            placeholder="Dê um título criativo à sua ideia"
            required
          >
        </div>

        <!-- Descrição -->
        <div class="mb-3">
          <label for="description" class="form-label fw-semibold text-white">
            <i class="bi bi-pencil-square me-1 text-info"></i>
            Descrição
          </label>
          <textarea 
            name="description" 
            id="description" 
            class="form-control border-0 bg-secondary text-light shadow-sm" 
            rows="4"
            placeholder="Descreva sua ideia com detalhes..."
            required
          ></textarea>
        </div>

        <!-- Categoria -->
        <div class="mb-4">
          <label for="category" class="form-label fw-semibold text-white">
            <i class="bi bi-tags-fill me-1 text-success"></i>
            Categoria
          </label>
          <input 
            type="text" 
            name="category" 
            id="category" 
            class="form-control border-0 bg-secondary text-light shadow-sm"
            placeholder="Ex: Sustentabilidade, Tecnologia, Educação..."
          >
        </div>

        <!-- Botão -->
        <div class="text-center">
          <button 
            type="submit" 
            class="btn btn-success btn-lg px-5 shadow-sm" 
            style="border-radius: 50px; transition: 0.3s;"
            onmouseover="this.style.transform='scale(1.05)';"
            onmouseout="this.style.transform='scale(1)';"
          >
            Criar Ideia
          </button>
        </div>
      </form>
    </div>
  </div>
</div>
